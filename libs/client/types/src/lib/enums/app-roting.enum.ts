export enum AppRoutes {
  ROOT = '/',
  LINKS = '/links',
  REDIRECT = '/r/:slug',
  REGISTER = '/register'
}

export enum AppRouteNames {
  HOME = 'HOME',
  LINKS = 'LINKS',
  REGISTER = 'Register'
}
