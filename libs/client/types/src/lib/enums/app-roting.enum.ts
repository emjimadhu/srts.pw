export enum AppRoutes {
  ROOT = '/',
  LINKS = '/links',
  REDIRECT = '/r/:slug',
  REGISTER = '/register',
  LOGIN = '/login'
}

export enum AppRouteNames {
  HOME = 'HOME',
  LINKS = 'LINKS',
  REGISTER = 'Register',
  LOGIN = 'Login'
}
